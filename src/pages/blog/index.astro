--- 
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE } from '../../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`Blog - ${SITE_TITLE}`} description="Tech tips & updates" />
	</head>
	<body>
		<Header />
		<main>
			<h1>Blog</h1>
			<h2>Recent Posts</h2>
			<div class="posts-grid">
				<a href="/blog/first-post" class="post-card">
					<img src="/CTHLogo-2-1024x908.jpg" alt="Fixing Slow WiFi" />
					<div>
						<h3>Fixing Slow WiFi</h3>
						<p>Step-by-step troubleshoot for faster home network. (Feb 4)</p>
					</div>
				</a>
				<a href="/blog/ai-tips" class="post-card">
					<img src="/CTHLogo-2-1024x908.jpg" alt="AI Productivity" />
					<div>
						<h3>AI for Everyday Productivity</h3>
						<p>Practical AI tips for work/home—no fluff. (Feb 4)</p>
					</div>
				</a>
			</div>
			<p style="text-align: center; margin: 2em 0;">More coming—subscribe or ☎️ (469) 844-0493 for custom advice!</p>

			<style>
				.posts-grid {
					display: grid;
					grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
					gap: 2em;
					max-width: 1200px;
					margin: 2em auto;
				}
				.post-card {
					display: flex;
					flex-direction: column;
					background: rgba(255,255,255,0.1);
					backdrop-filter: blur(10px);
					border-radius: 1rem;
					border: 1px solid rgba(255,255,255,0.2);
					overflow: hidden;
					text-decoration: none;
					color: inherit;
					transition: transform 0.3s, box-shadow 0.3s;
				}
				.post-card:hover {
					transform: translateY(-5px);
					box-shadow: 0 20px 40px rgba(0,0,0,0.3);
				}
				.post-card img {
					width: 100%;
					height: 200px;
					object-fit: cover;
				}
				.post-card div {
					padding: 1.5em;
				}
				.post-card h3 {
					font-size: 1.3em;
					margin: 0 0 0.5em;
				}
				.post-card p {
					margin: 0 0 1em;
					color: var(--gray);
				}
				@media (max-width: 768px) {
					.posts-grid { grid-template-columns: 1fr; gap: 1.5em; }
				}
			</style>
		</main>
		<Footer />
	</body>
</html>